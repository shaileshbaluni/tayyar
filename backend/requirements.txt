# Core API (always)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic-settings>=2.6.0
aiofiles>=24.1.0
httpx>=0.27.0
websockets>=13.0

# Pipeline 1 — Pipecat voice (install when Daily + keys are ready)
pipecat-ai[google,daily]>=0.0.105

# Pipeline 2 — post-interview (install on analysis worker)
# pip install faster-whisper
# pip install praat-parselmouth
# Optional: OpenFace 3.0 CLI — set OPENFACE3_EXECUTABLE (wrapper emitting openface_features.json)
# Optional: Py-Feat — pip install py-feat && set PYFEAT_ENABLED=1
