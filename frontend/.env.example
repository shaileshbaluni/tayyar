# Backend base URL — leave empty for local dev (uses Vite proxy)
# In production (Vercel), set to your Render gateway URL e.g. https://tayyar-gateway.onrender.com
VITE_API_BASE=

# Optional — only if backend ADMIN_API_KEY is set
# VITE_ADMIN_API_KEY=
